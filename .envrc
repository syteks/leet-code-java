# Use the user-local Temurin JDK 26 for this project (matches maven.compiler.release=26).
# direnv auto-loads this on `cd` into the repo. Run `direnv allow` once to enable.
export JAVA_HOME="$HOME/.local/jdks/jdk-26.0.1+8"
export PATH="$JAVA_HOME/bin:$PATH"
